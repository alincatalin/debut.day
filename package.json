{
  "name": "debut-day-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "cf:types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply debut-day-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply debut-day-db --remote",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.3",
    "astro": "^7.2.4"
  }
}
